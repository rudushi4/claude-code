# Claude Code Custom API Configuration
# Copy this file to .env and modify as needed

# ===========================================
# CUSTOM BASE URL (Proxy Server)
# ===========================================
# Use a different URL prefix for API calls, e.g., to use proxy servers
# The default prefix is https://api.anthropic.com
ANTHROPIC_BASE_URL=https://your-proxy-server.com/v1

# ===========================================
# API KEY
# ===========================================
# API key that is sent using the authorization header
# It defaults to the ANTHROPIC_API_KEY environment variable
ANTHROPIC_API_KEY=your-api-key-here

# Alternative: Use auth token
# ANTHROPIC_AUTH_TOKEN=your-auth-token

# ===========================================
# PROXY CONFIGURATION
# ===========================================
# HTTP/HTTPS proxy for network requests
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1

# Enable if proxy handles DNS resolution
# CLAUDE_CODE_PROXY_RESOLVES_HOSTS=true

# ===========================================
# API KEY HELPER (Dynamic Keys)
# ===========================================
# Script path that outputs API key dynamically
# CLAUDE_CODE_API_KEY_HELPER=/path/to/key-helper.sh

# TTL for cached API keys (milliseconds)
# CLAUDE_CODE_API_KEY_HELPER_TTL_MS=300000

# ===========================================
# DEBUGGING
# ===========================================
# Enable debug logging
# ANTHROPIC_LOG=debug

# ===========================================
# TELEMETRY
# ===========================================
# Disable telemetry when using custom endpoints
# DISABLE_TELEMETRY=true
# CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=true

# ===========================================
# TIMEOUTS
# ===========================================
# Bash command default timeout (milliseconds)
# BASH_DEFAULT_TIMEOUT_MS=30000

# Bash command max timeout (milliseconds)  
# BASH_MAX_TIMEOUT_MS=300000

# API timeout (milliseconds)
# API_TIMEOUT_MS=600000
